{
   "_id":"_design/casa",
   "language":"javascript",
   "views":{
      "applications":{
         "map":"function(doc){\n  if(doc.type != null && doc.type == 'application'){\n    emit(doc.title, doc)\n  }\n}"
      }
   }
}
